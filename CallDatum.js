
var datum="None";
function callDatum(datum,d)
{
var svg = d3.select("#callDatum").append("svg:svg");
activateDat="true";
tags.filter(function(d){
tag=d.indexOf(datum);
filterDatum(datum,tag)
{
filtDat[d[0]]=document.getElementById('filterDatum').innerHTML;
};
}); 
start();
};